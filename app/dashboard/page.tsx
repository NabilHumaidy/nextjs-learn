import { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Dashboard',
    description: 'Dashboard for Acme',
}

export default function Page() {
    return <p>Dashboard Page</p>;
}